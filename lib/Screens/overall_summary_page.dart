import 'package:flutter/material.dart';

class OverallSummaryPage extends StatelessWidget {
  const OverallSummaryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Summary')),
      body: const Center(child: Text('Overall summary placeholder')),
    );
  }
}
